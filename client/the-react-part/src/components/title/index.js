import { React, useState } from 'react';
import { Box } from 'theme-ui';

export const Title = () => {
  return (
    <Box sx={{ font: 'system-ui, sans-serif', fontSize: '40px', display: 'flex', justifyContent: 'center', mt: '30px' }}>
      EWU Grant's Office Manager
    </Box>
  );
};
